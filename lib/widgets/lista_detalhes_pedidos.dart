import 'package:flutter/material.dart';

class ListaDetalhesPedidos extends StatelessWidget {
  const ListaDetalhesPedidos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'ListaDetalhesPedidos is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
